{
  "name": "shipment_rates_api",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "build": "tsc",
    "watch": "tsc -w",
    "clean": "rimraf dist",
    "prestart": "npm run clean && npm run build",
    "start": "func start",
    "test": "echo \"No tests yet...\""
  },
  "dependencies": {
    "@azure/cosmos": "^4.0.0",
    "inversify": "^6.0.1",
    "node-fetch": "^3.3.2",
    "reflect-metadata": "^0.1.13",
    "shipengine": "^1.0.5"
  },
  "devDependencies": {
    "@azure/functions": "^3.0.0",
    "@types/node": "^16.x",
    "rimraf": "^5.0.0",
    "typescript": "^4.0.0"
  }
}
